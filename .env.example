# EliteNexus Backend - Environment Example
# Copy to .env and fill in values. Never commit .env to git.

NODE_ENV=development
PORT=8080

# MongoDB (required in production)
MONGO_URI=mongodb://127.0.0.1:27017/elitenexus

# Auth - magic-link (passwordless); JWT_SECRET and MAGIC_LINK_SECRET must be strong in production
JWT_SECRET=dev_secret_change_me_in_production
JWT_EXPIRES_IN=7d
MAGIC_LINK_SECRET=dev_magic_link_secret_change_me
AUTH_COOKIE_NAME=nexus_token
COOKIE_SECURE=false
COOKIE_SAME_SITE=lax
COOKIE_DOMAIN=

# Magic link base URL (login link in email). Use backend origin if same as API.
APP_URL=http://localhost:8080

# SMTP for sending magic-link emails (required for request-link to send mail)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@example.com

# CORS - comma-separated origins (production: only nexus.elitesolutionsnetwork.com)
CORS_ORIGIN=https://nexus.elitesolutionsnetwork.com,http://localhost:3000

# Admin seed (first deploy; passwordless — admin logs in via magic link)
ADMIN_EMAIL=admin@example.com

# Rate limiting (optional overrides)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=100
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=10

# Logging
# LOG_LEVEL=info
